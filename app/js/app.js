// Everything in Angular is split into modules. Modules are containers for different 'bits' of our application, like classes in an OOP application.
// Here, our 'top-level' Angular module is our application, which we will call toDoApp

var toDoApp = angular.module('toDoApp', []);
